class UpdateThemeSettingsAction {
  final int primaryColor;
  final int secondaryColor;
  final int cardColor;
  final int appBarColor;
  final int shadowColor;
  final int textColor;
  final int iconColor;

  UpdateThemeSettingsAction(this.primaryColor, this.secondaryColor, this.cardColor,
      this.appBarColor, this.shadowColor, this.textColor, this.iconColor);
}
