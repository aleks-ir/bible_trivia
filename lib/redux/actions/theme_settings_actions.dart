import 'dart:ui';

class UpdateThemeSettingsAction {
  final int primaryColor;
  final int secondaryColor;
  final int appBarColor;
  final int shadowColor;
  final int textColor;
  UpdateThemeSettingsAction(this.primaryColor, this.secondaryColor, this.appBarColor, this.shadowColor, this.textColor, );
}